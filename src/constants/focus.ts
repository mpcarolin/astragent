export const PHASE_ANGLE = 0.65;
export const ELEVATION = 0.25;
export const ZOOM_FACTOR = 8;

/** ecliptic-frame floor on the standoff, so a small body is not approached inside MIN_DISTANCE. */
export const MIN_APPROACH = 0.02;

export const DURATION_MS = 1200;
